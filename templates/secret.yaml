apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: default
type: Opaque
stringData:
  # Use stringData — k8s auto-encodes to base64
  password: changeme
  api-key: replace-with-real-key
